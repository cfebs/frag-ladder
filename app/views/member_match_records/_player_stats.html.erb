<div class="clickable button add_player">
    Add Player
</div>

<table class="player_table">
    <tr>
        <th>Player</th>
    <tr>
    <%= f.fields_for :member_match_records do |s| %>
    <tr class="player_row">
        <td>
            <%= s.collection_select :member_id, team.members.all, :id, :alias, :prompt => true %>
        </td>
    </tr>
    <% end %>
</table>
